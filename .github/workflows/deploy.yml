name: Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment"
        required: true
        type: choice
        options: [dev, staging, prod]
      strategy:
        description: "Deploy strategy"
        required: true
        type: choice
        options: [rolling, recreate, canary]
      ref_type:
        description: "branch or tag"
        required: true
        type: choice
        options: [branch, tag]
      ref_name:
        description: "Branch name or tag"
        required: true
        type: string
      release_id:
        description: "MAMS release id"
        required: true
        type: string
      mams_callback_url:
        description: "MAMS callback URL (optional)"
        required: false
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref_name }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get lowercase registry path
        run: |
          REGISTRY_PATH=$(echo "${{ github.actor }}/work-service" | tr '[:upper:]' '[:lower:]')
          echo "REGISTRY_PATH=$REGISTRY_PATH" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/${{ env.REGISTRY_PATH }}:${{ inputs.ref_name }}
          cache-from: type=registry,ref=ghcr.io/${{ env.REGISTRY_PATH }}:latest
          cache-to: type=inline

      - name: Simulate deployment
        run: |
          echo "Image pushed: ghcr.io/${{ env.REGISTRY_PATH }}:${{ inputs.ref_name }}"
          echo "Deploying to ${{ inputs.environment }} with strategy ${{ inputs.strategy }}"
          sleep 3

      - name: Callback to MAMS (success)
        if: success() && inputs.mams_callback_url != ''
        env:
          RELEASE_ID: ${{ inputs.release_id }}
          CALLBACK_URL: ${{ inputs.mams_callback_url }}
        run: |
          curl -sS -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -d "{\"release_id\":\"$RELEASE_ID\",\"status\":\"success\"}"

      - name: Callback to MAMS (failure)
        if: failure() && inputs.mams_callback_url != ''
        env:
          RELEASE_ID: ${{ inputs.release_id }}
          CALLBACK_URL: ${{ inputs.mams_callback_url }}
        run: |
          curl -sS -X POST "$CALLBACK_URL" \
            -H "Content-Type: application/json" \
            -d "{\"release_id\":\"$RELEASE_ID\",\"status\":\"failed\"}"